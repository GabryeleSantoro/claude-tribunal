# Publish a GitHub Release for every pushed semver tag. Version is always the tag
# that triggered the workflow (github.ref_name); plugin.json must match (without "v").
name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify plugin.json matches tag
        env:
          TAG_NAME: ${{ github.ref_name }}
        run: |
          strip_v="${TAG_NAME#v}"
          plugin="$(jq -r '.version' .claude-plugin/plugin.json)"
          if [ "$strip_v" != "$plugin" ]; then
            echo "::error title=Version mismatch::Tag $TAG_NAME implies version $strip_v but .claude-plugin/plugin.json has $plugin"
            exit 1
          fi

      - name: Build .skill artifact
        run: |
          mkdir -p dist
          cd /tmp && rm -rf tribunal_pkg && mkdir -p tribunal_pkg/deliberate
          cp "$GITHUB_WORKSPACE/skills/deliberate/SKILL.md" tribunal_pkg/deliberate/
          cp "$GITHUB_WORKSPACE/skills/deliberate/reference.md" tribunal_pkg/deliberate/
          cd tribunal_pkg && zip -r "$GITHUB_WORKSPACE/dist/claude-tribunal.skill" deliberate/
          echo "Built: $(ls -lh $GITHUB_WORKSPACE/dist/claude-tribunal.skill)"

      - name: Create GitHub Release (${{ github.ref_name }})
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          generate_release_notes: true
          prerelease: ${{ contains(github.ref_name, '-') }}
          files: dist/claude-tribunal.skill
